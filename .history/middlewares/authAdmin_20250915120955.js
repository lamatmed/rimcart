const { clerkClient } = require("@clerk/nextjs/server")

const  authAdmin = async (userId) =>{
try {
    if(!userId) return false
    const client = await clerkClient()
    const user = await clie
} catch (error) {
    
}
}